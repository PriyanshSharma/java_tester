package com.practice;

import java.util.Map;

public class AtlassianCodingTest {

	
	
	/*
	 * Imagine you are given a stream of val points consisting of 
	 * <timestamp, commodityPrice> you are supposed to return the maxCommodityPrice at 
	 * any point in time. The timestamps in the stream can be out of order, 
	 * or there can be duplicate timestamps, we need to update the commodityPrice at that 
	 * particular timestamp if an entry for the timestamp already exists.
	 * Create an in-memory solution tailored to prioritize frequent reads and writes 
	 * for the given problem statement.
	 * (4, 27)(6, 26)(4,25)(9, 25).
	 * getMaxCommodityPrice() => 27
	 */
	
	public static void main(String[] args) {
		
	}
	
	private static int getMaxCommodityPrice(Map<Integer, Integer> mapper) {
		
		
		return 0;
	}
	
}
